# MCP Server
mcp>=0.9.0

# API Client
openai>=1.0.0

# PDF Processing
pymupdf>=1.23.0

# Image Processing
Pillow>=10.0.0

# Environment Variables
python-dotenv

# YAML Configuration
PyYAML

# Web API
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6

# HTTP client (async)
httpx>=0.27.0

# PDF generation (Markdown → PDF)
weasyprint>=60.0

# QR code (for WeChat login in V2)
qrcode>=7.0

# AES decryption (for WeChat CDN in V2)
pycryptodome>=3.20.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-mock>=3.10.0
pytest-cov>=4.0.0
